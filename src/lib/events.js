import Phaser from 'phaser';

export default class EventManager {
    constructor(scene) {
        this.scene = scene;
        this.events = scene.events;
    }

    addEventListeners() {
        
    }
}